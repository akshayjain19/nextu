export type Testimonial = {
  id: string;
  name: string;
  role: string;
  testimonial: string;
  avatar?: string;
};

/** Placeholder structure — replace with real testimonials when available. */
export const testimonials: Testimonial[] = [];
